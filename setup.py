from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A Twitter bot that uses Markov models to generate new tweets based on different corpuses.',
    author='Ty Andrews',
    license='MIT',
)
